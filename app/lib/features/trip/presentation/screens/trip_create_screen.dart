import 'package:flutter/material.dart';

import '../../../../shared/widgets/empty_screen.dart';

class TripCreateScreen extends StatelessWidget {
  const TripCreateScreen({super.key});

  @override
  Widget build(BuildContext context) => const EmptyScreen(title: 'trip_create');
}

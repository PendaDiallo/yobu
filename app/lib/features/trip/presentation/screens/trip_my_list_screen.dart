import 'package:flutter/material.dart';

import '../../../../shared/widgets/empty_screen.dart';

class TripMyListScreen extends StatelessWidget {
  const TripMyListScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const EmptyScreen(title: 'trip_my_list');
}
